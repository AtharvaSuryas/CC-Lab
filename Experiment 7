printf("0x");
    for(int j=i-1;j>=0;j--)
        printf("%c",hex[j]);
}

int string_to_int(char*str){
    int result=0;
    for(int i=0;str[i]!='\0';i++){
        result=result*10+(str[i]-'0');
    }
    return result;
}
%}

%%
[0-9]+ {
    int num = string_to_int(yytext);
    decimal_to_hex(num);
}
.|\n    {ECHO;}
%%

int main(){
    yylex();
    return 0;
}

int yywrap(){return 1;}
